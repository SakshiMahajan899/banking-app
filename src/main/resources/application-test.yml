spring:
  application:
    name: backend-assignment

  data:
    mongodb:
      uri: mongodb://localhost:27017/banking-db  # Replace with your MongoDB URI
      database: banking-db  # Database name


management:
  endpoints:
    web:
      exposure:
        include: health,info

appUserName: user
password: password
